{
	"name": "@template/create-gasoline-monorepo-pnpm",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "turbo run build",
		"dev": "gasoline dev",
		"dev:all": "turbo run dev",
		"release": "pnpm run build && changeset publish"
	},
	"devDependencies": {
		"@biomejs/biome": "1.5.3",
		"@cloudflare/workers-types": "^4.0.0",
		"esbuild": "^0.20.2",
		"turbo": "^1.13.0",
		"typescript": "^5.0.0",
		"vitest": "^1.3.1",
		"wrangler": "^3.0.0"
	}
}
