{
	"name": "@template/create-gasoline-monorepo-npm",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "nx run-many --target=build",
		"dev": "nx run-many --target=dev",
		"release": "npm run build && changeset publish"
	},
	"devDependencies": {
		"@biomejs/biome": "1.5.3",
		"@cloudflare/workers-types": "^4.0.0",
		"@nx/js": "^17.0.0",
		"nx": "^17.0.0",
		"prettier": "^3.0.0",
		"tsup": "^8.0.1",
		"typescript": "^5.0.0",
		"vitest": "^1.3.1",
		"wrangler": "^3.0.0"
	},
	"workspaces": [
		"gasoline/*",
		"packages/*"
	]
}
